### GlossAPI runtime requirements

# Core pipeline deps
pandas>=1.3.0
numpy>=1.26,<3
python-dateutil>=2.8.2
pytz>=2021.1
scikit-learn==1.6.1
joblib>=1.0.0
dask>=2022.1.0
pyarrow>=7.0.0
aiohttp>=3.8.0
aiofiles>=23.0.0
ftfy>=6.0.0
tenacity>=8.0.0
tqdm>=4.67.0

# Docling extraction/layout stack
docling==2.81.0
pyyaml>=6.0

# Enrichment & JSON compression (required for Phase-2 math/code and JSON zstd)
pypdfium2>=4.0.0
zstandard>=0.22.0

# Optional: install Torch CUDA for GPU-backed Docling layout / enrichment
