"""Utility scripts shipped with GlossAPI.

This subpackage hosts lightweight CLI helpers that are invoked via
``python -m glossapi.scripts.<module>`` to keep multiprocessing entrypoints
`spawn`-safe even in constrained environments (AWS SageMaker, conda, etc.).
"""

